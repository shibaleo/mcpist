{
  "name": "@mcpist/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env.dev -- air",
    "build": "go build -o bin/server ./cmd/server",
    "start": "dotenv -e ../../.env.dev -- go run ./cmd/server",
    "test": "dotenv -e ../../.env.dev -- go test -v ./...",
    "lint": "golangci-lint run"
  }
}
