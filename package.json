{
  "name": "mcpist",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "start": "pnpm env:sync && supabase start && concurrently \"pnpm start:console\" \"pnpm start:server\" \"pnpm start:worker\"",
    "stop": "supabase stop",
    "start:console": "pnpm env:sync && pnpm --filter @mcpist/console dev",
    "start:server": "dotenv -e .env.local -- go run -C apps/server ./cmd/server",
    "start:worker": "pnpm --filter @mcpist/worker dev",
    "dev": "pnpm env:sync && concurrently -n console,server,worker -c blue,green,yellow \"pnpm start:console\" \"pnpm start:server\" \"pnpm start:worker\"",
    "dev:docker": "supabase start && docker compose --env-file .env.local up -d --build",
    "stop:docker": "docker compose down && supabase stop",
    "logs:docker": "docker compose logs -f",
    "docs:dev": "pnpm --filter @mcpist/docs dev",
    "docs:build": "pnpm --filter @mcpist/docs build",
    "docs:preview": "pnpm --filter @mcpist/docs preview",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "env:sync": "node scripts/sync-env.js"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.14.2",
    "concurrently": "^9.2.1",
    "dotenv-cli": "^8.0.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
